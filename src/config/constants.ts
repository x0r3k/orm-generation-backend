export const CONFIG_KEY = {
  app: 'app',
  db: 'db',
} as const;

export const ENVIRONMENT = {
  local: 'local',
  development: 'development',
  production: 'production',
  testing: 'testing',
} as const;
